# Environment
.env
.env.local
*.local

# Node
node_modules/
dist/
npm-debug.log*
yarn-error.log*

# Go
*.exe
mikrotik-go-service/mikrotik-go-service

# Python
__pycache__/
*.pyc

# Build artifacts
*.tgz
*.zip

# OS
.DS_Store

# IDE
.idea/
.vscode/

# TypeScript incremental build cache (must never be committed — a stale
# cache can trick `tsc --incremental` into emitting ZERO output files on a
# fresh clone, silently, with exit code 0).
*.tsbuildinfo
